rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all collections for team collaboration and seamless syncing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
